# VeNRA Training Environment Requirements (CORRECTED)
# Python 3.11.x on Ubuntu 24.04.3 LTS
# NVIDIA Driver with CUDA 13.1 support

# =============================================================================
# CRITICAL: PyTorch and triton are installed SEPARATELY first (see setup.sh)
# Do NOT install them via this requirements.txt
# =============================================================================

# Transformers Ecosystem
transformers==4.46.3      # Qwen2.5-Coder-3B support
datasets==2.21.0          # Latest stable
tokenizers==0.20.3        # Matches transformers
accelerate==0.34.2        # Multi-GPU and model loading support

# Parameter-Efficient Fine-Tuning
peft==0.13.2              # rsLoRA support
trl==0.12.2               # SFTTrainer with bug fixes

# Quantization - ALREADY INSTALLED via setup.sh
# bitsandbytes==0.43.3

# Experiment Tracking
wandb==0.18.7
python-dotenv==1.0.1

# Scientific Computing
numpy==1.26.4             # Pre-2.0 for compatibility
scipy==1.14.1
scikit-learn==1.6.1
pandas==2.2.3

# Tokenizer Support
sentencepiece==0.2.0
protobuf==4.25.5
einops==0.8.0
